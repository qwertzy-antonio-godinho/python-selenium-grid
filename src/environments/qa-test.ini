[server.grid]
protocol = http
hostname = localhost
port = 4450
url = ${server.grid:protocol}://${server.grid:hostname}:${server.grid:port}

[server.aut]
protocol = https
hostname = www.osnews.com
port = 443
url = ${server.aut:protocol}://${server.aut:hostname}:${server.aut:port}